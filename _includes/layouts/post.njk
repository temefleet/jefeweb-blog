---
layout: layouts/base.njk
templateClass: tmpl-post
---
<div class="markdown container mx-auto px-4 py-12">
  <div class="markdown__header border-b pb-2 mb-8 flex justify-between items-end">
    <h1 class="pr-4">{{ title }}</h1>

    <time class="text-sm" datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time>
  </div>

  {{ content | safe }}

  {%- set nextPost = collections.posts | getNextCollectionItem(page) %}
  {%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}
  {%- if nextPost or previousPost %}
  <hr>
  <ul>
    {%- if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
    {%- if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
  </ul>
  {%- endif %}
</div>
